import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
	fonts: {
		body: 'Inter, Montserrat, system-ui, sans-serif',
		heading: 'Inter, Montserrat, serif',
		mono: 'Menlo, monospace',
	},
});
